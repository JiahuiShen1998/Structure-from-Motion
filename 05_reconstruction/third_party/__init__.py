"""Vendored upstream COLMAP scripts, unmodified.

`database.py` and `read_write_model.py` are taken verbatim from COLMAP's
`scripts/python/` (BSD-3-Clause, ETH Zurich and UNC Chapel Hill) and keep their original
licence headers. They are kept in a separate package so it is obvious which code in this
stage is project work and which is upstream.
"""
